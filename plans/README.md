# Exo on Basilica

Read the single [unified implementation plan](EXO-IMPLEMENTATION-PLAN.md).

It contains the product design, all three repository scopes, shared contracts, exclusive agent ownership, parallel work schedule, dependency gates, validation criteria, and coordinator-maintained implementation ledger. It replaces the three earlier numbered plans; no other plan is required.

Status: implementation in progress. The unified plan contains current commits, validation evidence and outstanding G0–G4 acceptance work.

Branch: `docs/exo-implementation-plan`, based on `origin/main`. Worktree: `/Users/samueldare/code/cc/basilica/basilica-exo-plan`. Future implementation branches in all three repositories must also originate from main-based integration branches, not the unrelated current feature checkout.
